# 转化选项提示

在选择链接外使用 `<<transform-hint "horse" "softbrown">>` 显示转化名称。

第一个参数是框架翻译键，第二个参数是作者指定的 CSS 颜色类，例如 `blue`、`orange`、`gold` 或 `softbrown`。框架不指定统一颜色。

提示只显示转化名称，与原版动物转化提示一致。此宏只显示提示，不增加进度；关闭数值提示（`blindStatsEnabled`）时不显示。
